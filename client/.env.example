# Backend base URL (no trailing /v1). Example:
# VITE_API_URL=http://localhost:5000
#
# If you omit this, the dev server proxies /v1 → http://localhost:5000 (see vite.config.js).
# Production builds should set this to your real API origin.
